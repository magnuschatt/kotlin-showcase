ktor {
    deployment {
        port = 8080
        environment = development
        autoreload = true
        watch = [ ktor ]
    }

    application {
        id = showcase
        modules = [ chatt.showcase.kotlin.ktor.AppKt.main ]
    }
}
